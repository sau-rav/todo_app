import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // using this as global style sheet
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

